# PROD environment — see engine/.env.dev for the full explanation of how these
# per-environment files work. Fill in real values below.
#
# Running BDD scenarios against production is inherently higher-risk (data mutation,
# real traffic) — double check tag scoping before triggering a run against this one.

BASE_URL=
API_BASE_URL=
